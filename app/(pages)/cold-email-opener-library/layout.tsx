import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Cold Email Opener Library – eProd",
  description: "Curated cold email opening lines plus optional AI-generated variants.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
